---
name: cli-compression
description: Configure and test prompt compression from the CLI. Manage RTK filters, Caveman rules, stacked compression modes, and preview compression output with real prompts.
---
<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

Configure and test prompt compression from the CLI. Manage RTK filters, Caveman rules, stacked compression modes, and preview compression output with real prompts.

## Quick install

```bash
npm install -g AIRoute   # or: npx AIRoute
AIRoute --version
```

## Subcommands

### `compression`

**Example:**

```bash
AIRoute compression
```

### `compression status`

**Example:**

```bash
AIRoute compression status
```

### `compression configure`

**Flags:**

- `--engine <e>`
- `--caveman-aggressiveness <n>`
- `--rtk-budget <n>`
- `--language-pack <p>`

**Example:**

```bash
AIRoute compression configure
```

### `compression engine`

**Example:**

```bash
AIRoute compression engine
```

### `compression set <name>`

**Example:**

```bash
AIRoute compression set <name>
```

### `compression get`

**Example:**

```bash
AIRoute compression get
```

### `compression combos`

**Example:**

```bash
AIRoute compression combos
```

### `compression list`

**Example:**

```bash
AIRoute compression list
```

### `compression stats`

**Flags:**

- `--period <p>`

**Example:**

```bash
AIRoute compression stats
```

### `compression rules`

**Example:**

```bash
AIRoute compression rules
```

### `compression list`

**Example:**

```bash
AIRoute compression list
```

### `compression add`

**Flags:**

- `--replacement <r>`

**Example:**

```bash
AIRoute compression add
```

### `compression remove <id>`

**Flags:**

- `--yes`

**Example:**

```bash
AIRoute compression remove <id>
```

### `compression language-packs`

**Example:**

```bash
AIRoute compression language-packs
```

### `compression preview`

**Example:**

```bash
AIRoute compression preview
```
