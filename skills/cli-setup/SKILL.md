---
name: cli-setup
description: Run initial setup, configure global CLI settings, manage environment variables, check for updates, and configure autostart via the CLI setup and config commands.
---
<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

Run initial setup, configure global CLI settings, manage environment variables, check for updates, and configure autostart via the CLI setup and config commands.

## Quick install

```bash
npm install -g AIRoute   # or: npx AIRoute
AIRoute --version
```

## Subcommands

### `autostart`

**Example:**

```bash
AIRoute autostart
```

### `autostart enable`

**Example:**

```bash
AIRoute autostart enable
```

### `autostart disable`

**Example:**

```bash
AIRoute autostart disable
```

### `autostart toggle`

**Example:**

```bash
AIRoute autostart toggle
```

### `autostart status`

**Example:**

```bash
AIRoute autostart status
```

### `config`

Show or update CLI tool configuration

**Example:**

```bash
AIRoute config
```

### `config list`

List all CLI tools and config status

**Flags:**

- `--json`

**Example:**

```bash
AIRoute config list
```

### `config get <tool>`

Show current config for a tool

**Flags:**

- `--json`

**Example:**

```bash
AIRoute config get <tool>
```

### `config set <tool>`

Write config for a tool

**Flags:**

- `--model <model>`
- `--non-interactive`
- `--yes`

**Example:**

```bash
AIRoute config set <tool>
```

### `config validate <tool>`

Validate config format without writing

**Flags:**

- `--model <model>`
- `--json`

**Example:**

```bash
AIRoute config validate <tool>
```

### `config opencode`

Generate OpenCode config (alias for 

**Flags:**

- `--model <model>`
- `--non-interactive`
- `--yes`

**Example:**

```bash
AIRoute config opencode
```

### `config lang`

**Example:**

```bash
AIRoute config lang
```

### `config get`

**Flags:**

- `--json`

**Example:**

```bash
AIRoute config get
```

### `config set <code>`

**Flags:**

- `--force`

**Example:**

```bash
AIRoute config set <code>
```

### `config list`

**Flags:**

- `--json`

**Example:**

```bash
AIRoute config list
```

### `env`

Show and manage environment variables

**Example:**

```bash
AIRoute env
```

### `env show`

Show current environment variables

**Flags:**

- `--json`

**Example:**

```bash
AIRoute env show
```

### `env get <key>`

Get a single environment variable

**Example:**

```bash
AIRoute env get <key>
```

### `env set <key> <value>`

Set an environment variable (current session only)

**Example:**

```bash
AIRoute env set <key> <value>
```

### `setup`

**Flags:**

- `--password <value>`
- `--add-provider`
- `--provider <id>`
- `--provider-name <name>`
- `--api-key <value>`
- `--default-model <model>`
- `--provider-base-url <url>`
- `--test-provider`
- `--non-interactive`
- `--list`

**Example:**

```bash
AIRoute setup
```

### `update`

**Flags:**

- `--check`
- `--apply`
- `--changelog`
- `--dry-run`
- `--no-backup`
- `--yes`

**Example:**

```bash
AIRoute update
```
