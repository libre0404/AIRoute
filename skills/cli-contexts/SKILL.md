---
name: cli-contexts
description: Manage context engineering configurations, RTK filter sets, and conversation sessions from the CLI. Apply context-relay settings and inspect active context pipelines.
---
<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

Manage context engineering configurations, RTK filter sets, and conversation sessions from the CLI. Apply context-relay settings and inspect active context pipelines.

## Quick install

```bash
npm install -g AIRoute   # or: npx AIRoute
AIRoute --version
```

## Subcommands

### `context-eng`

**Example:**

```bash
AIRoute context-eng
```

### `context-eng analytics`

**Flags:**

- `--period <p>`

**Example:**

```bash
AIRoute context-eng analytics
```

### `context-eng caveman`

**Example:**

```bash
AIRoute context-eng caveman
```

### `context-eng config`

**Example:**

```bash
AIRoute context-eng config
```

### `context-eng show`

**Example:**

```bash
AIRoute context-eng show
```

### `context-eng set`

**Flags:**

- `--aggressiveness <n>`
- `--max-shrink-pct <n>`
- `--preserve-tags <list>`

**Example:**

```bash
AIRoute context-eng set
```

### `context-eng rtk`

**Example:**

```bash
AIRoute context-eng rtk
```

### `context-eng config`

**Example:**

```bash
AIRoute context-eng config
```

### `context-eng show`

**Example:**

```bash
AIRoute context-eng show
```

### `context-eng set`

**Flags:**

- `--token-budget <n>`
- `--reserve-pct <n>`

**Example:**

```bash
AIRoute context-eng set
```

### `context-eng filters`

**Example:**

```bash
AIRoute context-eng filters
```

### `context-eng list`

**Example:**

```bash
AIRoute context-eng list
```

### `context-eng add`

**Flags:**

- `--priority <n>`
- `--action <a>`

**Example:**

```bash
AIRoute context-eng add
```

### `context-eng remove <id>`

**Flags:**

- `--yes`

**Example:**

```bash
AIRoute context-eng remove <id>
```

### `context-eng test`

**Example:**

```bash
AIRoute context-eng test
```

### `context-eng raw-output <id>`

**Example:**

```bash
AIRoute context-eng raw-output <id>
```

### `context-eng combos`

**Example:**

```bash
AIRoute context-eng combos
```

### `context-eng list`

**Example:**

```bash
AIRoute context-eng list
```

### `context-eng get <id>`

**Example:**

```bash
AIRoute context-eng get <id>
```

### `context-eng assignments <id>`

**Example:**

```bash
AIRoute context-eng assignments <id>
```

### `contexts`

**Example:**

```bash
AIRoute contexts
```

### `contexts list`

List all contexts

**Example:**

```bash
AIRoute contexts list
```

### `contexts add <name>`

Add a new context

**Flags:**

- `--api-key <k>`
- `--api-key-stdin`
- `--access-token <t>`
- `--access-token-stdin`
- `--scope <s>`
- `--description <d>`

**Example:**

```bash
AIRoute contexts add <name>
```

### `contexts use <name>`

Switch active context

**Example:**

```bash
AIRoute contexts use <name>
```

### `contexts current`

Show the active context (server, auth, scope)

**Flags:**

- `--name-only`

**Example:**

```bash
AIRoute contexts current
```

### `contexts show <name>`

Show context details

**Example:**

```bash
AIRoute contexts show <name>
```

### `contexts remove <name>`

Remove a context

**Flags:**

- `--yes`

**Example:**

```bash
AIRoute contexts remove <name>
```

### `contexts rename <old> <new>`

Rename a context

**Example:**

```bash
AIRoute contexts rename <old> <new>
```

### `contexts export`

Export contexts to JSON

**Flags:**

- `--out <path>`
- `--no-secrets`

**Example:**

```bash
AIRoute contexts export
```

### `contexts import <file>`

Import contexts from a JSON file

**Flags:**

- `--merge`

**Example:**

```bash
AIRoute contexts import <file>
```

### `sessions`

**Example:**

```bash
AIRoute sessions
```

### `sessions list`

**Flags:**

- `--user <u>`
- `--kind <k>`
- `--active`
- `--limit <n>`

**Example:**

```bash
AIRoute sessions list
```

### `sessions show <sessionId>`

**Example:**

```bash
AIRoute sessions show <sessionId>
```

### `sessions expire <sessionId>`

**Flags:**

- `--yes`

**Example:**

```bash
AIRoute sessions expire <sessionId>
```

### `sessions expire-all`

**Flags:**

- `--yes`

**Example:**

```bash
AIRoute sessions expire-all
```

### `sessions current`

**Example:**

```bash
AIRoute sessions current
```
